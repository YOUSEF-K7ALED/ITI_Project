// Component: BookButton (Person 3)
// - Used on EventDetails page
// - On click: calls bookingsService.createBooking(eventId)
// - Handles errors: "You already booked this event", "No available seats"
// - On success: show confirmation, redirect to My Bookings
