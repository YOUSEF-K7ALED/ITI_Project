// Page: My Bookings (Person 3)
// - Fetch bookings: bookingsService.getMyBookings()
// - Render list of <BookingCard /> (event title, date, price, status)
// - Each card has "View" and "Cancel" actions
