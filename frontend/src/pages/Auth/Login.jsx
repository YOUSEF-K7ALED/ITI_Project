// Page: Login (Person 1)
// - Form fields: email, password
// - On submit: call authService.login(email, password)
// - On success: save token (context/AuthContext), redirect to Events page
// - Show validation errors / API error messages
